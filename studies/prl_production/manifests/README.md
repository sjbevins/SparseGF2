# Run manifests

Each launch writes an immutable JSON manifest containing the resolved grid,
RNG schema, package version, Git state, and command line.  Runtime state files
are generated alongside it and ignored by Git.
